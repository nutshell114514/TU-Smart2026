# 使用方式

将`trained.tflite`和`labels.txt`放到摄像头的板子里，然后运行`deploy.py`.

> [!NOTE]
> `deploy.py` 有三种设定好的参数模式：`stable`、`balanced`、`fast`。
> 三者分别从保守到激进，默认为 `fast`。实际上发现 `stable` 和 `balanced` 表现都很差，所以选择 `fast`。
> 修改时只需改第 67 行的 `PROFILE_NAME`；此外，也可以自己在 `PROFILES` 里新增并命名参数模式。


# 日志

- 2026/4/2 高鹏：这是用汽院旧实验室数据集和风洞基地新数据集混合训练得到的模型。我还没有实际测试过，但从 F1 分数来看，应该可以使用。

- 2026/4/2 21：44 高鹏：这份模型在不设置sensor的亮度的原图情况下，仅网球认不出来，识别其他物体非常准确，且移动目标也能跟得住；发给陈墨涵做ppt用的两份图片就来自这个模型。
  但是问题有两个：
  1. 识别不出来网球，必须要低亮度才行，但是如果整体亮度调到300，会让模型什么都认不出来（目前三份可能可用的FOMO模型都是这样），可能的解决方法如下：
    - 找一个亮度值，让模型既能认出网球又能认出其他物体
    - 拷贝两份，一份原图高亮度识别其他物体、一份低的找网球；但问题在于低亮度会有很多错识别的
  2. 最莫名其妙的问题，我真的不知道为什么，说起来也像我编的一样，我自己也不愿意相信。模型是两面派，一种情况下如上所述（除了网球外）很准确，另一种情况下表现很糟糕，几乎是乱识别。明明是同一份模型，同一份代码，也没有什么随机因素在。目前猜测可能导致这个变化的原因：
    - 模型真的有一些内部算子上的问题，导致模型没有连续性/是混沌的，对输入极其敏感，一点点的差别会导致结果的完全不同。这是最严重的可能性，但是其实我觉得不大可能，因为这个也是edgeimpulse训练的，和之前的都一样，不大可能有模型的问题。
    - gptcodex给我写的部署代码有问题，使得实际推理时有时候用的原图，有时候用的是降低亮度图，而显示在画面上的始终是原图；这个我觉得可能性较大。
    - openmvIDE的问题，我修改代码保存后的运行会表现不一样。
    这个问题我想解决的可能性还是挺大的，毕竟不大可能是第一种原因那样的致命问题。
  接下来就按照上面的方法都试一下。



# 模型信息

- 项目 ID: `946522`
- 项目名称: `gpt/mixed(TL->strict)`
- 训练作业 ID: `46046378`
- 模型配置: `mixed_strict_rgb96_e120_lr1e3_b32`
- Impulse 名称: `mixed_strict_rgb96_e120_lr1e3_b32_RGB_96`
- 项目类型: `object_detection`
- 标签:
  - `background`
  - `car`
  - `sandbag`
  - `teddy`
  - `tennis`
- 数据集划分:
  - Training: `1489`
  - Testing: `344`
- 输入尺寸: `96 x 96`
- Resize mode: `squash`
- 学习块: `keras-object-detection`
- 训练处理器: `cpu`
- 最佳 Precision: `0.99`
- 最佳 Recall: `0.99`
- 最佳 F1: `0.99`
- 对应日志行: `11   0.03429  0.02672 0.99      0.99   0.99`
